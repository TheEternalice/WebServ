git config user.email "LucLgdm@users.noreply.github.com"

git log --pretty=format:"%h - %an <%ae>"

git filter-branch -f --env-filter '
if [ "$GIT_COMMITTER_EMAIL" = "lde-merc@paul-f5Br3s5.clusters.42paris.fr" ]; then
    export GIT_COMMITTER_EMAIL="LucLgdm@users.noreply.github.com"
    export GIT_AUTHOR_EMAIL="LucLgdm@users.noreply.github.com"
fi
' -- --all

git push --force


# Activer l'environnement base (quand vous voulez utiliser nvcc)
conda activate base

# Désactiver
conda deactivate
